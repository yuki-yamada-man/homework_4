<div class="container">
  <div class="row">
    <%= render "users/info", user:@user, book:@book_new %>
    <div class="col">
      <h1>detail of user</h1>
      <table>
        <thead>
          <tr>
            <th>title</th>
            <th>opinion</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @books.each book do |book| %>
          <tr>
            <td>
              <%= link_to ,user_path(@user.id) do %>
               <%= attachment_image_tag @user, :profile_image, fallback:"no_image.jpg", size:"40x40" %>
               <%= @user.name %>
              <% end %>
            </td>
            <td><%= link_to book.title, book_path(book.id) %></td>
            <td><%= book.body %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>